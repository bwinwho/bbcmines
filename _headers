# Cloudflare Pages reads this file; GitHub Pages ignores it.
# bbcmines.pages.dev serves the same build as bbcmines.com — keep the
# preview/pages.dev host out of the index so it never competes with the
# custom domain for the "bbc mines" brand search.
https://bbcmines.pages.dev/*
  X-Robots-Tag: noindex
