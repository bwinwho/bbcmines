Drop real photography here, then point to it from assets/js/config.js (MEDIA).
Never reference a file in this folder directly from HTML — config.js is the
only place a media path may appear (BLUEPRINT.md §0, rule 2).

Art direction — BLUEPRINT.md §4.4:
- Macro mineral texture: fill-frame, raking side light, shot on white.
- The leases: wide, high-horizon, desaturated, documentary — no golden hour.
- Material in transit: loaded tippers, jumbo bags, stacked yard.
- Never: stock excavators, handshakes, globes, circuit-board gradients.

Until real files land, every image slot renders the CSS-only placeholder
wash defined in site.css — that is expected, not a bug.
